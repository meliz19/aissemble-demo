apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aissemble-machine-learning-inference
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: aissemble-demo
    server: {{ .Values.spec.destination.server }}
  project: default
  source:
    path: aissemble-demo-deploy/src/main/resources/apps/aissemble-machine-learning-inference
    repoURL: {{ .Values.spec.repo }}
    targetRevision: {{ .Values.spec.targetRevision }}
    {{ if .Values.spec.helm.valueFiles }}
    helm:
      valueFiles:
        {{- range .Values.spec.helm.valueFiles }}
        - {{ . }}
        {{- end }}    
    {{ end }}
  {{- with .Values.spec.syncPolicy }}
  syncPolicy:
    {{- toYaml . | nindent 4 }}
  {{- end }}